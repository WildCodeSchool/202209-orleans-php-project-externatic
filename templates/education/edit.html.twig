{% extends 'dashboard_layout.html.twig' %}

{% block title %}
{{ parent() ~ ' | Edition d\'une formation' }}
{% endblock %}

{% block sideNav %}
	{% include './component/_sideBar_dashboard.html.twig' %}
{% endblock %}

{% block content %}
	<div class="container">
		<div class="row justify-content-center">
			<div class="col-10">
				<div class="d-flex justify-content-between">
					<h1>Edition de la formation</h1>
                    {{ include('education/_delete_form.html.twig') }}
                </div>
				<div class="mt-5 form-container">
                {{ include('education/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
				</div>
			</div>
		</div>
	</div>

{% endblock %}
