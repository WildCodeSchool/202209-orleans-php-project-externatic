<div class="col-lg-6">

	<div class=" card card-background p-2">

		<div class=" m-2">
			<div class="card-body">
				<h2 class="fs-4 card-title card-offer-title">{{ candidate.user.firstname }}  {{ candidate.user.lastname }}</h2>
			</div>
			<div class="d-flex justify-content-between m-2">
				<h3 class="fs-6">{{ candidate.user.email }}
				</h3>
				<a href="{{ path('app_candidate_show', {id : candidate.id}) }}" class="btn btn-primary px-6 py-1">Voir</a>
			</div>
		</div>

	</div>
</div>
