<div class="col-lg-6">

	<div class=" card card-background p-2">

		<div class=" m-2">
			<div class="fs-6">{{offer.city}}
				({{offer.postalCode}})
			</div>
			<div class="card-body">
				<h2 class="fs-4 card-title card-offer-title">{{offer.title}}</h2>
			</div>
			<div class="d-flex justify-content-between m-2">
				<h3 class="fs-4">{{offer.annualWage/1000}}K€/ an
				</h3>
				<a href="#" class="btn btn-primary px-6 py-1">Voir</a>
			</div>
		</div>

	</div>
</div>
